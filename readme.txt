# Install
## Fork/clone
```
  git clone git@github.com:alexandre-nadin/nucleux.git "${USER}-nux"
  cd "${USER}-nux"
  git submodule update --init --recursive
```

Tries to mimick the /usr file hierarchy.
http://www.pathname.com/fhs/pub/fhs-2.3.html#PURPOSE18

